import {StyleSheet} from 'react-native';

const NewUserFormStyles = StyleSheet.create({
    container: {
        marginHorizontal: 10,
        marginVertical: 20
    }
});

export default NewUserFormStyles;